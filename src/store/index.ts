import { createStore } from 'vuex'
import AdminModule from "@/store/AdminModule";

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    Admin: AdminModule
  }
})
